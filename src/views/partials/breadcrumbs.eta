<nav class="breadcrumbs" aria-label="Breadcrumb">
  <ol>
    <% it.items.forEach(function(item) { %>
      <li><% if (item.path !== undefined) { %><a href="<%= item.path %>"><%= item.name %></a><% } else { %><span aria-current="page"><%= item.name %></span><% } %></li>
    <% }) %>
  </ol>
</nav>
