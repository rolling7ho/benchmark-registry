NODE_ENV=development
HOST=127.0.0.1
PORT=3000
DATABASE_URL=postgresql://benchmark_registry:change-me@localhost:5432/benchmark_registry
# Optional dedicated HMAC key. Generate at least 32 random bytes and keep it secret.
# When omitted, the server-only DATABASE_URL is used as the key.
# FEEDBACK_RATE_LIMIT_SECRET=replace-with-a-long-random-secret
# Configure both values to enable the protected /admin/feedback routes.
# ADMIN_USERNAME=registry-admin
# ADMIN_PASSWORD=replace-with-a-long-unique-password
