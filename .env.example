NODE_ENV=development
HOST=127.0.0.1
PORT=3000
DATABASE_URL=postgresql://benchmark_registry:change-me@localhost:5432/benchmark_registry
# Configure both values to enable the protected /admin/feedback routes.
# ADMIN_USERNAME=registry-admin
# ADMIN_PASSWORD=replace-with-a-long-unique-password
